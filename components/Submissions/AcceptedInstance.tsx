import { AcceptedAward } from "@/lib/types"

type Props = {
    data: AcceptedAward;
    onBack: () => void;
}

export default function AcceptedFormInstance({ data, onBack }: Props) {
    return (
        <div></div>
    )
}
